# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Local test artefacts
_selftest.html

# Never commit secrets. config.js holds only the publishable key,
# which is safe in a public repo; anything secret belongs in the
# Supabase dashboard as an Edge Function environment variable.
.env
.env.*

# Photographic originals for tools/make-headshot.ps1. The 23KB crop it
# produces is committed; the 19MB source is not. Git history is forever, and
# these are licensed stock files that belong wherever the licence lives —
# not in a public repository.
tools/*.jpg
tools/*.jpeg
